#include <stdio.h>
#include "main.h"
/* Short description */

/**
 * strlen - Function
 * @s: Parameter 1
 *
 * Return: void
 */
 int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
