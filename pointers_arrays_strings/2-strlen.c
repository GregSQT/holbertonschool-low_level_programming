#include <stdio.h>
#include <main.h>

/**
 * Exervices en C 
 *
 */

int string_length(char* given_string)
{
	int length = 0;
	int _strlen(char *s);
		while (*s != '\0') {
		length++;
		s++;
	}

    return length;
}
